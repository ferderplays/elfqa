package ney.kuoku.mixins.blocks;

import net.minecraft.block.*;

public class BlockMixin extends elfqa {
    if(this.loadedWorld())
    {
        do {
            this.loadBlock(all);
        }
    }
}