package ney.kuoku.mixins.entity;

import net.minecraft.entity.*;

public class EntityMixin extends elfqa {
    if(this.loadedWorld())
    {
        do {
            this.loadEntity(all);
        }
    }
}