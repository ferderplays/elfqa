package me.ferderplays.elfqa.features.modules.joke;

public class announcements extends elfqa {
    public announcements() {
        super("annoucements","jk lol", GUI.category.joke, true, true, false);
        String a = private static final Settings long < type1 > = this.setAnnouncement("i placed %placed_blocks% blocks");
        STring b = private static final Settings long < type2 > = this.setAnnouncement("i ran %distance% blocks");
    }
    void enable() {
        if (c === a) {
            do {
                String c = this.sendAnnouncement(a);
            }
        }
        if (c === b) {
            do {
                String c = this.sendAnnouncement(b);
            }
        }
    }
}